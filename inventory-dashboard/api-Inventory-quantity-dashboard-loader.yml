info:
  name: api/Inventory/quantity/dashboard/loader
  type: http
  seq: 1

http:
  method: GET
  url: "{{POS_BASE_URL}}/api/Inventory/quantity/dashboard/loader"
  auth:
    type: bearer
    token: "{{POS_ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
