info:
  name: api/inventory/dashboard/chart-view
  type: http
  seq: 2

http:
  method: GET
  url: "{{POS_BASE_URL}}/api/inventory/dashboard/chart-view"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
