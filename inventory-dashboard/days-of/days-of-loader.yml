info:
  name: days-of/loader
  type: http
  seq: 2

http:
  method: GET
  url: "{{POS_BASE_URL}}/api/inventory/dashboard/days-of/loader"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
