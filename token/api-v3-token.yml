info:
  name: api/v3/token
  type: http
  seq: 1

http:
  method: POST
  url: "{{POS_BASE_URL}}/api/v3/token"
  headers:
    - name: debug
      value: "1"
  body:
    type: json
    data: |-
      {
        "client_id": "{{POS_USERNAME}}",
        "client_secret": "{{POS_PASSWORD}}"
      }
  auth: inherit

runtime:
  actions:
    - type: set-variable
      phase: after-response
      selector:
        expression: res.body.access_token
        method: jsonq
      variable:
        name: POS_ACCESS_TOKEN
        scope: runtime

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
